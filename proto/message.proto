syntax = "proto3";

package belphegor;

option go_package = "internal/types/proto";

// Message represents clipboard data and its associated metadata
message Message {
  int64 ID = 1;
  bytes Data = 2;
  Mime MimeType = 3;
}

enum Mime {
  TEXT = 0;
  IMAGE = 1;
}
